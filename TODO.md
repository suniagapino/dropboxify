* `dropboxify -L` to create links from the top level Dropbox content to target ($HOME) dir
* Python Windows-compatible port using mklink
* Python unit tests of all versions
* Travis integration
* Docs, usage
